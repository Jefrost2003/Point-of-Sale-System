@model IEnumerable<IT15_INATO_POS.Models.SecurityLog>
@using IT15_INATO_POS.Models
@using Microsoft.AspNetCore.Identity
@inject UserManager<ApplicationUser> UserManager
@{
    ViewData["Title"] = "Security Logs";
}

<div class="container-fluid mt-4">
    <div class="row mb-4">
        <div class="col-md-6">
            <h2 class="fw-bold" style="color: #1E293B;">
                <i class="bi bi-shield-lock me-2" style="color: #4F46E5;"></i>Security Logs
            </h2>
            <p class="text-muted">Authentication and security events - Login/logout, user creation/deletion, password/role changes, failed logins</p>
        </div>
        <div class="col-md-6 text-end">
            <a asp-action="Index" class="btn btn-outline-secondary">
                <i class="bi bi-arrow-left me-1"></i>Back to Users
            </a>
        </div>
    </div>

    <!-- Date Range & User Filter -->
    <div class="card border-0 shadow-sm mb-4">
        <div class="card-body">
            <form method="get" class="row g-3 align-items-end">
                <div class="col-md-3">
                    <label class="form-label fw-bold" style="color: #4F46E5;">
                        <i class="bi bi-calendar-range me-1"></i>Start Date
                    </label>
                    <input type="date" name="startDate" class="form-control" value="@ViewBag.StartDate">
                </div>
                <div class="col-md-3">
                    <label class="form-label fw-bold" style="color: #4F46E5;">
                        <i class="bi bi-calendar-range me-1"></i>End Date
                    </label>
                    <input type="date" name="endDate" class="form-control" value="@ViewBag.EndDate">
                </div>
                <div class="col-md-4">
                    <label class="form-label fw-bold" style="color: #4F46E5;">
                        <i class="bi bi-person me-1"></i>User
                    </label>
                    <select name="userId" class="form-select">
                        <option value="">All Users</option>
                        @if (ViewBag.Users != null)
                        {
                            foreach (var user in ViewBag.Users as List<ApplicationUser> ?? new List<ApplicationUser>())
                            {
                                <option value="@user.Id" selected="@(ViewBag.SelectedUserId == user.Id)">
                                    @user.FullName - @user.Email
                                </option>
                            }
                        }
                    </select>
                </div>
                <div class="col-md-2">
                    <button type="submit" class="btn w-100" style="background: #4F46E5; color: white; border: none;">
                        <i class="bi bi-search me-1"></i>Filter
                    </button>
                </div>
            </form>
        </div>
    </div>

    <div class="card border-0 shadow-sm">
        <div class="card-body p-0">
            <div class="table-responsive">
                <table class="table table-hover mb-0">
                    <thead style="background: #F8FAFC;">
                        <tr>
                            <th>Timestamp</th>
                            <th>User</th>
                            <th>Role</th>
                            <th>Event Type</th>
                            <th>Description</th>
                            <th>IP Address</th>
                    </thead>
                    <tbody>
                        @foreach (var log in Model)
                        {
                            var userRoles = log.User != null ? await UserManager.GetRolesAsync(log.User) : new List<string>();
                            <tr>
                                <td>@log.Timestamp.ToString("MMM dd, yyyy HH:mm:ss")</td>
                                <td>
                                    @if (log.User != null)
                                    {
                                        <strong style="color: #1E293B;">@log.User.FullName</strong>
                                        <br />
                                        <small class="text-muted">@log.User.Email</small>
                                    }
                                    else
                                    {
                                        <span class="text-muted">System / Unknown</span>
                                    }
                                </td>
                                <td>
                                    @if (log.User != null && userRoles.Any())
                                    {
                                        foreach (var role in userRoles)
                                        {
                                            <span class="badge me-1" style="background: #4F46E5;">@role</span>
                                        }
                                    }
                                    else
                                    {
                                        <span class="text-muted">-</span>
                                    }
                                </td>
                                <td>
                                    @switch (log.EventType)
                                    {
                                        case "LoginSuccess":
                                            <span class="badge" style="background: #10B981;">
                                                <i class="bi bi-box-arrow-in-right me-1"></i>@log.EventType
                                            </span>
                                            break;
                                        case "LoginFailed":
                                            <span class="badge" style="background: #EF4444;">
                                                <i class="bi bi-x-circle me-1"></i>@log.EventType
                                            </span>
                                            break;
                                        case "Logout":
                                            <span class="badge" style="background: #64748B;">
                                                <i class="bi bi-box-arrow-right me-1"></i>@log.EventType
                                            </span>
                                            break;
                                        case "PasswordChanged":
                                            <span class="badge" style="background: #F59E0B;">
                                                <i class="bi bi-key me-1"></i>@log.EventType
                                            </span>
                                            break;
                                        case "AccountLocked":
                                            <span class="badge" style="background: #1E293B;">
                                                <i class="bi bi-lock-fill me-1"></i>@log.EventType
                                            </span>
                                            break;
                                        case "RoleChanged":
                                            <span class="badge" style="background: #8B5CF6;">
                                                <i class="bi bi-person-badge me-1"></i>@log.EventType
                                            </span>
                                            break;
                                        case "UserCreated":
                                            <span class="badge" style="background: #10B981;">
                                                <i class="bi bi-person-plus me-1"></i>@log.EventType
                                            </span>
                                            break;
                                        case "UserDeleted":
                                            <span class="badge" style="background: #EF4444;">
                                                <i class="bi bi-person-x me-1"></i>@log.EventType
                                            </span>
                                            break;
                                        case "EmailChanged":
                                            <span class="badge" style="background: #3B82F6;">
                                                <i class="bi bi-envelope me-1"></i>@log.EventType
                                            </span>
                                            break;
                                        default:
                                            <span class="badge" style="background: #6366F1;">@log.EventType</span>
                                            break;
                                    }
                                </td>
                                <td>@log.Description</td>
                                <td><code style="font-size: 12px;">@log.IpAddress</code></td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>